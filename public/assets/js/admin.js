(()=>{document.querySelectorAll('form[data-confirm]').forEach(form=>form.addEventListener('submit',event=>{if(!confirm(form.dataset.confirm))event.preventDefault()}));const form=document.querySelector('[data-unsaved]');if(!form)return;let dirty=false;form.addEventListener('input',()=>dirty=true);form.addEventListener('submit',()=>dirty=false);addEventListener('beforeunload',event=>{if(dirty){event.preventDefault();event.returnValue=''}});const source=form.querySelector('[data-slug-source]'),target=form.querySelector('[data-slug-target]');source?.addEventListener('input',()=>{if(!target.dataset.edited)target.value=source.value.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')});target?.addEventListener('input',()=>target.dataset.edited='1');const markdown=form.querySelector('[data-markdown]'),preview=form.querySelector('.preview-pane');let timer;markdown?.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(()=>{preview.textContent=markdown.value||'Preview is empty. Markdown will be safely rendered when saved.'},180)});document.querySelectorAll('[data-tab]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-tab]').forEach(b=>b.classList.toggle('active',b===button));form.querySelector('.split-editor').dataset.mobileTab=button.dataset.tab}))})();
