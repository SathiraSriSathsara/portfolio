(()=>{const time=document.querySelector('#local-time');if(time){const update=()=>{try{time.textContent=new Intl.DateTimeFormat('en-LK',{timeZone:time.dataset.timezone,hour:'2-digit',minute:'2-digit',timeZoneName:'short'}).format(new Date());time.dateTime=new Date().toISOString()}catch{}};update();setInterval(update,60000)}document.querySelectorAll('pre').forEach(pre=>{const button=document.createElement('button');button.type='button';button.className='copy-code';button.textContent='Copy';button.addEventListener('click',async()=>{await navigator.clipboard.writeText(pre.innerText);button.textContent='Copied';setTimeout(()=>button.textContent='Copy',1500)});pre.insertAdjacentElement('beforebegin',button)})})();
